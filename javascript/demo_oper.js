
/*
//i did
function indexCalc(height,weight)
{
    return weight/(height*height);
}

function indexSep(index)
{
    if(index>0 && index<18.4){
        console.log("slim");
    }
    
    else if(index>=18.5 && index<24.9){
        console.log("normal");
    }
    
    else if(index>=25 && index<29.9){
        console.log("overweight");
    }
    
    else if(index>=30 && index<34.9){
        console.log("obese");
    }
}
var h1=prompt("Can's height");
var w1=prompt("Can's weight");

index=indexCalc(h1,w1);
console.log(index);
indexSep(index);

var h2=prompt("Ada's height");
var w2=prompt("Ada's weight");

index=indexCalc(h2,w2);
console.log(index);
indexSep(index);

//toFixed() yuvarlama yapılıyor içi boş olduğunda
*/